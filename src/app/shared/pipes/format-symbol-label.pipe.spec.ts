import { FormatSymbolLabelPipe } from './format-symbol-label.pipe';

describe('FormatSymbolLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSymbolLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
